# ML dependencies for Step 17 — Severity Prediction
# Install with: pip install -r requirements-ml.txt

pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8

# Optional — HuggingFace (disabled by default, HF_SEVERITY_ENABLED=false)
# Uncomment and install only when explicitly enabling HF zero-shot:
# transformers>=4.40
# torch>=2.2
# datasets>=2.19
# huggingface_hub>=0.23
